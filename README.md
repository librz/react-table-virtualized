# react-table-virtualized

Virtual & responsive table with built-in infinite scrolling & column filter 

## Available Scripts

1. `yarn start` this starts dev server

2. `yarn build` builds the app for production to the `build` folder
